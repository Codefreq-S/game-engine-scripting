using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class CameraRotate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //if(Input.GetKeyDown(KeyCode.LeftArrow)) // check for if the left arrow is pressed
        //{
        //    transform.Rotate(Vector3.left * Time.deltaTime * 800); // If left arrow is pressed, rotate camera leftward
        //}
        //if (Input.GetKeyDown(KeyCode.RightArrow)) // check for if the left arrow is pressed
        //{
        //    transform.Rotate(Vector3.right * Time.deltaTime * 800); // If left arrow is pressed, rotate camera leftward
        //}
    }
}
